import { index, integer, sqliteTable, text } from 'drizzle-orm/sqlite-core'
import { workspaces } from './workspaces'
export const materials = sqliteTable('materials', { id: text('id').primaryKey(), workspaceId: text('workspace_id').notNull().references(() => workspaces.id, { onDelete: 'cascade' }), name: text('name').notNull(), mediaType: text('media_type').notNull(), pageCount: integer('page_count').notNull(), createdAt: integer('created_at').notNull() }, (table) => [index('materials_workspace_idx').on(table.workspaceId)])
export const materialChunks = sqliteTable('material_chunks', { id: text('id').primaryKey(), materialId: text('material_id').notNull().references(() => materials.id, { onDelete: 'cascade' }), pageNumber: integer('page_number').notNull(), content: text('content').notNull() }, (table) => [index('material_chunks_material_page_idx').on(table.materialId, table.pageNumber)])
