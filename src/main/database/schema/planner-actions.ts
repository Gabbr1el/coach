import { sql } from 'drizzle-orm'
import { check, index, integer, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core'

export const plannerActions = sqliteTable('planner_actions', {
  id: text('id').primaryKey(), originMessageId: text('origin_message_id').notNull(), label: text('label').notNull(), contextVersion: integer('context_version').notNull().default(0), idempotencyKey: text('idempotency_key').notNull(), type: text('type', { enum: ['workspace.prepare', 'workspace.create', 'deadline.create', 'routine.add', 'academic-life.save', 'academic-life.transition'] }).notNull(), status: text('status', { enum: ['proposed', 'applying', 'applied', 'rejected', 'obsolete'] }).notNull().default('proposed'), payloadJson: text('payload_json').notNull(), resultJson: text('result_json'), createdAt: integer('created_at').notNull(), resolvedAt: integer('resolved_at'),
}, (table) => [check('planner_actions_type_check', sql`${table.type} in ('workspace.prepare','workspace.create','deadline.create','routine.add','academic-life.save','academic-life.transition')`), check('planner_actions_status_check', sql`${table.status} in ('proposed','applying','applied','rejected','obsolete')`), uniqueIndex('planner_actions_idempotency_idx').on(table.idempotencyKey), index('planner_actions_status_created_idx').on(table.status, table.createdAt)])
