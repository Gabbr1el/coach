export type LearningDifficulty = 'low' | 'medium' | 'high'
export type LearningConfidence = 'low' | 'medium' | 'high'
export interface TopicLearningState { workspaceId: string; topicId: string; evidenceCount: number; assessments: number; correctFirstTry: number; correctAfterHelp: number; incorrect: number; hintsUsed: number; reinforcementEvents: number; exercisesCompleted: number; lessonsCompleted: number; difficultyLevel: LearningDifficulty; masteryEstimate: number | null; confidence: LearningConfidence; needsReview: boolean; lastPracticedAt: number | null; lastAssessedAt: number | null; reasons: string[]; updatedAt: number }
export interface LearningEvidence { type: 'CHECKPOINT_ANSWERED' | 'TOPIC_COMPLETED' | 'HELP_USED'; correct?: boolean; attempt?: number; hintUsed?: boolean; reinforcementUsed?: boolean; exerciseCompleted?: boolean; occurredAt: number }
export function emptyTopicLearningState(workspaceId: string, topicId: string, now: number): TopicLearningState { return { workspaceId, topicId, evidenceCount: 0, assessments: 0, correctFirstTry: 0, correctAfterHelp: 0, incorrect: 0, hintsUsed: 0, reinforcementEvents: 0, exercisesCompleted: 0, lessonsCompleted: 0, difficultyLevel: 'low', masteryEstimate: null, confidence: 'low', needsReview: false, lastPracticedAt: null, lastAssessedAt: null, reasons: [], updatedAt: now } }
export function applyLearningEvidence(current: TopicLearningState, evidence: LearningEvidence): TopicLearningState { const next = { ...current, reasons: [...current.reasons], evidenceCount: current.evidenceCount + 1, updatedAt: evidence.occurredAt }; if (evidence.type === 'HELP_USED') next.hintsUsed++; else if (evidence.type === 'CHECKPOINT_ANSWERED') { next.assessments++; next.lastAssessedAt = evidence.occurredAt; if (!evidence.correct) next.incorrect++; else if ((evidence.attempt ?? 1) === 1 && !evidence.hintUsed && !evidence.reinforcementUsed) next.correctFirstTry++; else next.correctAfterHelp++; if (evidence.hintUsed) next.hintsUsed++; if (evidence.reinforcementUsed) next.reinforcementEvents++ } else { next.lessonsCompleted++; next.lastPracticedAt = evidence.occurredAt; if (evidence.exerciseCompleted) next.exercisesCompleted++ } const positive = next.correctFirstTry * 3 + next.correctAfterHelp + next.exercisesCompleted * 2; const negative = next.incorrect * 3 + next.hintsUsed + next.reinforcementEvents * 2; const assessed = next.assessments + next.exercisesCompleted; next.confidence = assessed >= 6 ? 'high' : assessed >= 3 ? 'medium' : 'low'; next.masteryEstimate = assessed < 3 ? null : Math.max(0, Math.min(100, Math.round(50 + (positive - negative) * 7))); next.difficultyLevel = negative >= 8 || next.incorrect >= 3 ? 'high' : negative >= 3 ? 'medium' : 'low'; next.needsReview = next.difficultyLevel === 'high' || (next.difficultyLevel === 'medium' && (next.masteryEstimate ?? 0) < 70); next.reasons = [`${next.incorrect} checkpoints incorretos`, `${next.hintsUsed} dicas`, `${next.reinforcementEvents} reforços`, `${next.correctFirstTry} acertos de primeira`, `${next.exercisesCompleted} exercícios concluídos`]; return next }
export function shouldReplan(previous: TopicLearningState, next: TopicLearningState): boolean { return previous.difficultyLevel !== next.difficultyLevel || previous.needsReview !== next.needsReview || (previous.masteryEstimate === null) !== (next.masteryEstimate === null) || (previous.masteryEstimate !== null && next.masteryEstimate !== null && Math.abs(previous.masteryEstimate - next.masteryEstimate) >= 15) || next.lessonsCompleted > previous.lessonsCompleted }
