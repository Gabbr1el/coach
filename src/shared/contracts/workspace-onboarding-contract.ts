import { z } from 'zod'
import type { AcademicSubjectContext, AcademicSubjectDeclaration, DeclaredAcademicLevel } from './academic-subject-context-contract'
export const fundamentalsAnswerSchema = z.enum(['yes', 'no', 'unknown'])
export const implementationLanguageSchema = z.enum(['python', 'c', 'java', 'javascript', 'typescript', 'other'])
export const analyzeWorkspaceTopicInputSchema = z.object({ topic: z.string().trim().min(1).max(120), diagnosticAnswer: z.string().trim().max(1000).optional(), fundamentals: fundamentalsAnswerSchema.optional(), implementationLanguage: implementationLanguageSchema.optional() }).strict()
export interface WorkspaceTopicAnalysis { readonly analysisToken: string; readonly revision: number; readonly topic: string; readonly canonicalSubject: string; readonly canonicalFocus: string; readonly canonicalContext: string; readonly objective: string; readonly needsDiagnostic: boolean; readonly needsFundamentals: boolean; readonly isProgramming: boolean; readonly fundamentals: z.infer<typeof fundamentalsAnswerSchema> | null; readonly implementationLanguage: z.infer<typeof implementationLanguageSchema> | null; readonly needsImplementationLanguage: boolean; readonly question: string | null; readonly contextSource: 'academic-context' | 'general-memory' | 'diagnostic' | 'none'; readonly declaredLevel: DeclaredAcademicLevel | null; readonly declaredKnowledge: readonly string[]; readonly declaredDifficulties: readonly string[]; readonly declarations: readonly string[]; readonly evidence: readonly string[]; readonly goals: readonly string[]; readonly relatedContexts: readonly AcademicSubjectContext[]; readonly localKnowledgeProjection: string }
export interface WorkspaceOnboardingApi { analyze(input: z.infer<typeof analyzeWorkspaceTopicInputSchema>): Promise<WorkspaceTopicAnalysis>; replaceAcademicContext(input: AcademicSubjectDeclaration): Promise<AcademicSubjectContext> }
