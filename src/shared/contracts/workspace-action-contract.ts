import { z } from 'zod'
import { workspaceIdSchema } from './workspace-contract'
export const workspaceActionTypeSchema = z.enum(['plan.recalculate', 'plan.complete', 'plan.load.adjust', 'plan.availability.set', 'notes.add', 'roadmap.preview-materials'])
export const workspaceActionInputSchema = z.discriminatedUnion('type', [z.object({ workspaceId: workspaceIdSchema, type: z.literal('plan.recalculate'), arguments: z.object({}).strict() }).strict(), z.object({ workspaceId: workspaceIdSchema, type: z.literal('plan.complete'), arguments: z.object({ itemId: z.uuid() }).strict() }).strict(), z.object({ workspaceId: workspaceIdSchema, type: z.literal('plan.load.adjust'), arguments: z.object({ dateKey: z.iso.date(), deltaMinutes: z.number().int().min(-1440).max(1440).refine((value) => value !== 0) }).strict() }).strict(), z.object({ workspaceId: workspaceIdSchema, type: z.literal('plan.availability.set'), arguments: z.object({ weekday: z.number().int().min(0).max(6), minutes: z.number().int().min(0).max(1440), confirmedGlobal: z.literal(true) }).strict() }).strict(), z.object({ workspaceId: workspaceIdSchema, type: z.literal('notes.add'), arguments: z.object({ content: z.string().trim().min(1).max(4000) }).strict() }).strict(), z.object({ workspaceId: workspaceIdSchema, type: z.literal('roadmap.preview-materials'), arguments: z.object({ materialIds: z.array(z.uuid()).min(1).max(20), instruction: z.string().trim().min(1).max(2000).optional() }).strict() }).strict()])
export interface WorkspaceActionResult { id: string; workspaceId: string; type: z.infer<typeof workspaceActionTypeSchema>; status: 'succeeded' | 'failed' | 'needs_confirmation'; message: string; persisted: boolean; result: unknown }
